# Copy this file to `.env` (same folder) and put your real key in it.
# `.env` is gitignored and is read at startup; a real exported environment
# variable always wins over this file.
ANTHROPIC_API_KEY=sk-ant-your-key-here
